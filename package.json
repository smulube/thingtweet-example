{
  "name": "thingtweet",
  "version": "1.0.0",
  "description": "Simple example - moving tweet logic to backend to remove api keys from the browser.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sam Mulube",
  "license": "ISC",
  "dependencies": {
    "express": "^4.12.4",
    "request": "^2.56.0"
  }
}
